{
  "name": "jevcache",
  "version": "0.1.0",
  "description": "OpenAI-compatible proxy: Jev decides same-intent cache hits so you skip expensive chat calls.",
  "type": "module",
  "bin": { "jevcache": "./dist/cli.js" },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "test": "vitest run",
    "eval": "tsx scripts/eval.ts",
    "demo": "tsx scripts/load_demo.ts"
  },
  "engines": { "node": ">=20" },
  "license": "MIT",
  "dependencies": {
    "@hono/node-server": "^1.14.1",
    "better-sqlite3": "^11.9.1",
    "hono": "^4.7.5"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9"
  }
}
